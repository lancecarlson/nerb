<h1>Users</h1>

<table>
  <tr>
		<th>Email</th>
  </tr>

<% for user in @users %>
  <tr>
		<td><%= user.email %></td>
		<td><%= link_to "Edit", url(:edit_admin_user, user) %>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_admin_user) %>
